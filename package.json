{
  "name": "peering-manager-mcp-server",
  "version": "1.0.0",
  "description": "Read-only MCP server for Peering Manager",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "peering-manager-mcp-server": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npx tsc",
    "prepare": "npm run build",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
